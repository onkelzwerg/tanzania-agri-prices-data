# Source PDFs

The original Weekly Market Bulletin PDFs are not published in this repository
yet; they will be added here (as `sources/<year>/<file>.pdf`) at a later
point.

Until then, `data/provenance.csv` records the exact file name and SHA-256 of
the bulletin each week was extracted from, so the provenance chain stays
verifiable once the PDFs are uploaded.
